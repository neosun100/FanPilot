# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] — 2026-09-14

### 新增

- **温度口径三档，菜单栏可直接切换**（`temp_source`）
  | 档 | 相对最高核 | 手感 |
  |---|---|---|
  | `max` 最高核心 | — | 最保守，风扇最早升速 |
  | `average` 全核平均（默认） | 低约 3~13 °C | 性能核与能效核折中 |
  | `min` 最低核心 | 低约 20~25 °C | 最安静，风扇最晚升速 |

  温差之所以大，是因为两个核簇差距悬殊：实测**性能核 72~84 °C，能效核仅 58~62 °C**。
  面板里三个温度同时列出，并标出谁在驱动曲线（★）、谁在管紧急判据（!）。

- **紧急判据用独立设置 `emergency_source`**，不跟随温度口径
  🔴 若它也用 `min`，实测意味着**最高核 110 °C、平均 95 °C 时仍不触发** ——
  那不是保护被削弱，是等于关掉。所以两个口径分开，并在菜单里**显式显示**，不做隐藏行为。
  想让它跟随，把两个值改成一样即可。
  ⚠️ 无论怎么设，macOS/SoC 自身的硬件过热保护始终生效。

### 文档

- 新增 `docs/SMC-RESEARCH.md` §3.1 **温度读数可信度验证**（四项独立验证）：
  手工核对 IEEE-754 字节级解码 · 与第三方 App 独立实现交叉验证 ·
  用外接硬件传感器的环境温度做**物理反证**排除华氏度 · 空闲期下界反证。
  🩸 顺带查明一个会让人困惑的真实偏差：其它温度 App 可能显示比本项目**低约 10 °C**，
  那不是单位错，而是它们把多个键（含偏凉的风扇区传感器）**混合并平滑**后显示。
  两个数都对，口径不同；混合平滑值低于真实结温，不适合当控制输入。
- `SMC-RESEARCH.md` §5 标注为「实现前的计划」（最终未采用 SMJobBless/XPC），
  §6 从「未解决清单」改为**验证状态表**（多数项已由实测确证）。

### 测试

145 项（单元 93 + 验收 25 + E2E/回归 27）。新增三档口径与紧急口径独立性的断言，
含把已知后果钉成可执行断言：
`⚠️[量化] 若紧急也用 min：最高核 110°C、平均 95°C 时仍不触发 —— 这就是把两个口径拆开的理由`

## [1.0.1] — 2026-09-13

### 修复

- **`Makefile` 缺少头文件依赖**（构建体系的静默腐化风险）
  `src/fanpilotd` 的规则只列了 `fanpilotd.c`，没列它 `#include` 的 `src/fanlogic.h`。
  而 `tests/unit_logic` 的规则**列了**这个头文件。
  ⇒ 这个不对称很危险：改 `fanlogic.h` 后，**测试用新逻辑跑绿，而发布的守护是旧逻辑编的**。
  纯逻辑全在这个头文件里（曲线、夹取、限幅、故障状态机），所以这个漏洞正对着最要紧的代码。

### 说明

本版与 1.0.0 的**运行时行为完全相同**，只修构建依赖。
（顺带确认：clang 构建**不可复现**——同一份源码产出的二进制哈希不同，内嵌 UUID/路径所致。
所以「哈希不一致」不能用来判断「装的是不是旧代码」，该看 mtime 与源码内容。）

## [1.0.0] — 2026-09-13

首个正式版本。macOS 菜单栏风扇自适应温控，145 项测试全绿。

### 功能

- **自适应温控**：23 个 `Tp*` 核心温度传感器取最大值，五层控制链
  （`max()` → EMA 15s → 分段曲线 → 非对称限幅 → 死区）连续调节两个风扇
- **转速下限可调**：菜单栏 6 档（1500~4000 RPM），改完即刻生效
- **曲线自适应重铺**：抬高下限时整条曲线按 `[下限, 硬件上限]` 重新铺开，
  而不是被夹平出死区；并**按两个风扇各自的硬件上限分别重铺**（5349 / 5777 RPM）
- **菜单栏显示**：两行（最热温度 / 平均转速），固定宽度 23pt 不横跳，
  四种状态（正常 / 紧急 / 风扇故障 / 固件接管）用自绘几何标记区分
- **风扇故障检测**：实际转速持续低于目标 55% 达 12 秒即告警，
  并给 12 秒爬升宽限避免把「正在加速」误判成故障
- **开机自启开关**：菜单栏内可切换（用户域 LaunchAgent）
- **配置自动重载**：守护监视配置 mtime，无特权 App 改完配置即刻生效，无需信号或提权

### 安全设计

- 键白名单：只允许写 `F0md` / `F1md` / `F0Tg` / `F1Tg`，其余一律拒绝
- 值域校验：目标转速夹在**运行时读到的** `F*Mn`~`F*Mx` 内，不硬编码
- 配置视为**不可信输入**：`emergency_temp` 硬夹 70~95 °C，
  紧急路径忽略用户 `max_rpm` 只受硬件上限约束
- `flock` 单实例锁：防止两个实例抢写同一组 SMC 寄存器
- 三层失效安全：`SIGTERM` → 交还固件；`SIGKILL`/崩溃 → launchd `KeepAlive` 约 1s 接管；
  传感器读失败 → 立即交还固件

### 资源占用（实测）

| 指标 | 预算 | 实测 |
|---|---:|---:|
| 守护 CPU（2s 轮询） | < 0.3% | **0.100%** |
| 守护 RSS | < 8 MB | 5.66 MB |
| 亚 2ms 定时器 | < 5 /s | **0.00 /s** |
| 唤醒次数 | < 5 /s | 0.66 /s |
| SMC 写入（稳态） | 0 | **0** |

### 测试

`make test` —— 133 项：单元 81 + 验收 25 + E2E/回归 27（含 10 条按真实 bug 编号的回归）。

### 已知限制

- 仅在 **MacBook Pro M5 Max (Mac17,6) / macOS 26.6.2** 上验证过。
  不硬编码机型参数（风扇数取自 `FNum`，上下限取自 `F*Mn`/`F*Mx`），
  理论上适用于有可写 `F*Tg` 键的 Apple Silicon Mac，但未在其他机型实测。
- 与其他风扇控制软件**互斥**：两者会抢写同一组 SMC 寄存器。安装脚本会检测并拒绝安装。
